# Graph Communities

[English](README.md)

Graph Communities 会把 Obsidian 的黑白关系图谱变成一张按知识点组织的彩色知识地图。
插件为每个有效文件确定一个主主题和一个主子分类，节点只显示该主子分类的稳定颜色。
次要主题只保留为关系、搜索和悬停上下文，不混入节点颜色，也不参与主题占比。
结构性文件不进入可见图谱；剩余连线按源节点颜色到目标节点颜色绘制。

仓库的 [`mcp/`](mcp/) 目录还包含一个可选的只读知识网关。它不会把整个 Vault 交给 AI，而是返回带来源、
受 token 预算约束的小型证据包：默认只检索稳定核心知识；项目、参考资料和历史语料必须显式选择。
Obsidian 图谱插件仍然完全本地运行，也不依赖这个网关。

## 主要功能

- 在本地完整读取每篇 Markdown 笔记，识别学术、项目、参考资料及 Prompt/Skill 等用途。
- 从 `.codex/graph/topic-manifest.json` 读取可随硬盘移动的主题体系和显式配色；缺失或无效时使用内置离线兜底。
- 为每篇文章生成多标签知识画像，不再把文章名称或目录名称当成主要索引。
- 每个有效文件只使用一个主子分类颜色；次要主题不改变颜色和主题占比。
- 内置中性兜底主题，覆盖研究与学习、心理学、语言与沟通、交互设计、AI 与计算、数据治理、产品、市场和硬件等知识点。
- 识别大语言模型、AI 系统、Agent、语音、合规、硬件、市场与产品策略等主题。
- 将 Inbox、Library、Resources 等通用存放目录视为位置，而不是分类名称。
- 将 Product Manager 资料合并为四个紧凑分类：PM Strategy & Discovery、PM Execution & Growth、PM Data & Tools、PM Prompts。
- 突出 PM Skill 包中的主 `SKILL.md` 定义；重复包与辅助资料只降低视觉比重但保留分类色，图例仍会显示“主版本数 / 总数”。
- PM 按关键词保留一份 canonical Skill 或 Prompt/Workflow 代表，归入四个低权重子分类；重复仓库和辅助文件不进入图谱。
- 其他文章按正文知识点分类；桌面目录名只作为缺少正文证据时的兜底信息。
- 七个中性父主题使用高区分度基础色：AI 与计算、自动化与工具、研究与学习、数据与治理、硬件与机器人、产品与商业、设计与体验。
- 使用正文、标签、别名、标题层级、链接及低权重路径提示作为分类信号。
- 支持 `AI`、`LLM`、`ASR` 或自定义项目名等优先关键词。
- 将 README 类、AGENTS/系统文件、macOS `._*` AppleDouble 元数据、生成内容、空文件、纯导航、frontmatter 排除项和完全重复副本排除出分析、计数、可见节点及相关连线。
- 优先选择有实际主题含义的笔记作为代表节点，而不是导航文件。
- 为不同大型枢纽分配适合深色主题的不同主色。
- 链接和次要主题只增强关系，不改变笔记的唯一主色。
- 不同端点使用真实的“源颜色→目标颜色”渐变；同色端点使用实色；渲染器不支持纹理时安全回退为两端中间色。
- 同时支持全局图谱和局部图谱。
- 可选图例会显示全部活跃主题与子分类、有效文件数和占比；“主要主题”只做自然断点建议，不限制主题数量。
- 图谱文件节点默认显示为“父文件夹 / 文件名”，例如 `Projects / Plan`，避免同名文件无法区分。
- 图谱节点与分类图例会双向联动：打开节点时选中对应分类；点击左侧分类时，右侧对应节点保持原始分类色和 100% 不透明度，内部连线同步强化。
- 支持父分类与子分类联动：点击主题父分类会聚焦全部子类，打开具体笔记时会同时标记父分类和对应子分类。
- 打开图谱节点时会显著提亮目标节点和对应分类、强化相连边，并通过降低透明度弱化其他节点，但保留其原本色相；主动点击分类时会进一步聚焦该分类。
- 所有计算都在本地完成，不修改笔记内容。

## 视觉规则

```text
橙色主题节点 ─ 橙色→蓝色端点渐变 ─ 蓝色主题节点
```

节点大小仍由 Obsidian 控制，插件只改变颜色：

- **父主题色块**：使用高纯度基础色，只保留在图例父主题行。
- **节点颜色**：只表示该文件唯一的主子分类。
- **子分类色阶**：同一父主题下保持关联，同时彼此和父色可辨。
- **渐变方向**：遵循原生连线的源节点到目标节点。
- **柔和中性色**：表示孤立或暂时无法分类的笔记。

## 交互式聚焦

- 悬停图谱节点时，图例会立即预览并标记对应分类；点击节点并打开笔记后，该分类会保持选中状态，节点也会进一步突出。
- 点击图例中的分类，右侧该分类的全部节点会保持原始分类色和 100% 不透明度，内部连线保持原色并强化；无关节点和连线会显著变淡。此操作不改变节点大小、坐标或图谱布局。
- 再次点击当前分类，或运行 **Graph Communities: Clear graph community focus**，即可恢复完整图谱。
- 当前选中的笔记名称会显示在分类图例上方。

## 安装方式

### 从 Release 手动安装

1. 从最新 Release 下载 `main.js`、`manifest.json` 和 `styles.css`。
2. 新建 `<知识库>/.obsidian/plugins/graph-communities/` 文件夹。
3. 将三个文件复制到该目录。
4. 重启 Obsidian。
5. 进入 **设置 → 第三方插件**，启用 **Graph Communities**。

### 本地开发安装

Obsidian 插件本身除了 Node.js 外没有其他构建或运行依赖。可选 MCP 网关使用 pnpm：

```bash
npm run verify
node scripts/install-local.mjs /path/to/vault --enable

cd mcp
pnpm install --frozen-lockfile
pnpm run build
pnpm test
```

安装新构建后需要重启或重新加载 Obsidian。

## 推荐初始设置

| 设置 | 默认值 | 作用 |
| --- | ---: | --- |
| 最大社区数量 | 24 | 只限制关系聚类兜底，不限制语义主题数量或图例行数 |
| 最小社区规模 | 2 | 合并小碎片或将其显示为中性色 |
| 主题感知聚类 | 开启 | 综合本地正文用途、主题、元数据与链接 |
| 优先主题关键词 | 空 | 可选的用户自定义关键词，用于强化聚类与命名 |
| 内容分类凝聚力 | 5.0 | 让用途与主题相同的笔记保持聚集 |
| 主题相似度影响 | 1.4 | 让推断标签与本地元数据相似的笔记更接近 |
| 聚类分辨率 | 1.0 | 越低社区越少越大，越高社区越多越细 |
| 关系混合强度 | 0.52 | 只控制旧版关系色兜底，不改变已分类语义节点 |
| 混合距离 | 4 | 只供旧版关系色兜底使用的链接层数 |
| 弱化外围节点 | 0.18 | 让大型枢纽更加突出 |
| 图谱标签显示父文件夹 | 开启 | 将文件节点显示为“父文件夹 / 文件名”，不改变文件真实路径 |

大量调整链接后，可以运行命令：
**Graph Communities: Recompute graph communities**。
插件也会在元数据索引完成或 Markdown 文件变化后自动重新计算。

## 算法原理

1. 通过 Obsidian 本地知识库 API 完整读取每篇 Markdown，并按文件版本缓存。
2. 从正文与元数据推断“学术 / 项目 / 参考资料”等用途，以及多个主题标签。
3. 用维护的知识点本体对正文、标题层级、别名和标签评分；每篇文章最多保留八个知识点及命中证据词。
4. 将 PM 内容合并为四个子分类，每个关键词只保留一份 canonical 代表并降低图谱权重。
5. 将通用目录和文件名降为兜底提示，而不是主要分类依据。
6. 综合有效文件之间的知识点重合、语义相似度和链接，同时保留项目与学术多样性。
7. 在建立关系和计算占比前，先从有效文件集合中移除结构性文件与重复副本。
8. 优先解析 frontmatter 覆盖，再为每个可分类文件确定唯一主主题与主子分类。
9. 节点只使用主子分类的显式颜色；次要主题仅作为关系和搜索元数据。
10. 渲染器支持时把原生连线绘制为端点渐变，否则使用安全的中间色回退。

该模型会在本地读取完整 Markdown 正文，但不调用云端嵌入模型，也不会上传知识库内容。

## 隐私与安全

- 全部计算在 Obsidian 本地完成。
- 不发送网络请求，不收集分析数据，不需要 API Key。
- 不创建、修改、移动或删除笔记。
- 插件设置使用 Obsidian 标准插件数据机制保存。
- 禁用或卸载插件后，会恢复主题原本的节点颜色。
- GitHub Release 只包含 `main.js`、`manifest.json` 和 `styles.css`，不会打包 Vault 笔记、生成索引、本地路径或插件设置。
- 发布边界和漏洞报告方式见 [SECURITY.md](SECURITY.md)。

## 兼容性说明

Obsidian 暂未通过公开插件 API 提供逐节点着色能力，因此本插件与其他图谱着色插件一样，
需要访问图谱渲染器的内部节点和连线对象。未来 Obsidian 升级后可能暂时需要兼容更新；
即使出现兼容问题，也只会导致颜色不显示，不会影响知识库内容。

## 开发与测试

```bash
# 从源码生成可安装的 main.js
npm run build

# 运行语法检查和确定性单元测试
npm run verify

# 对真实知识库运行只读冒烟测试
node scripts/vault-smoke.mjs /path/to/vault
```

第二大脑的分阶段路线见 [`docs/SECOND-BRAIN-ROADMAP.md`](docs/SECOND-BRAIN-ROADMAP.md)：先解决检索边界，再处理分散来源，最后建设跨模型记忆。

## 许可证

MIT

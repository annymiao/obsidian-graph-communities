# Graph Communities

[English](README.md)

Graph Communities 会把 Obsidian 的黑白关系图谱变成一张具有结构含义的彩色知识地图。
插件自动识别连接紧密的笔记社区，为每个主要枢纽分配不同颜色，再沿链接关系传播并混合颜色。
因此，关系相似的笔记颜色更接近；连接多个社区的边界笔记会呈现自然的混合色。

## 主要功能

- 根据 Obsidian 已解析的内部链接自动识别知识社区。
- 将每个社区中连接度最高的笔记选为大型枢纽。
- 为不同大型枢纽分配适合深色主题的不同主色。
- 沿邻接关系传播颜色，使关系越紧密的节点颜色越相近。
- 对连接多个社区的桥接节点进行颜色混合。
- 根据两端节点颜色绘制连线，并弱化跨社区连线。
- 同时支持全局图谱和局部图谱。
- 可选图例会显示各社区的枢纽名称和节点数量。
- 所有计算都在本地完成，不修改笔记内容。

## 视觉规则

```text
蓝色枢纽 ─ 蓝色节点 ─ 蓝紫桥接节点 ─ 紫色节点 ─ 紫色枢纽
```

节点大小仍由 Obsidian 控制，插件只改变颜色：

- **主色**：表示所属知识社区。
- **颜色相似度**：表示图关系的相似程度。
- **混合色**：表示连接多个社区的边界节点。
- **柔和中性色**：表示孤立或暂时无法分类的笔记。

## 安装方式

### 从 Release 手动安装

1. 从最新 Release 下载 `main.js`、`manifest.json` 和 `styles.css`。
2. 新建 `<知识库>/.obsidian/plugins/graph-communities/` 文件夹。
3. 将三个文件复制到该目录。
4. 重启 Obsidian。
5. 进入 **设置 → 第三方插件**，启用 **Graph Communities**。

### 本地开发安装

项目除了 Node.js 外没有其他构建或运行依赖：

```bash
npm run verify
node scripts/install-local.mjs /path/to/vault --enable
```

安装新构建后需要重启或重新加载 Obsidian。

## 推荐初始设置

| 设置 | 默认值 | 作用 |
| --- | ---: | --- |
| 最大社区数量 | 9 | 限制主要枢纽颜色数量 |
| 最小社区规模 | 3 | 合并小碎片或将其显示为中性色 |
| 聚类分辨率 | 1.0 | 越低社区越少越大，越高社区越多越细 |
| 关系混合强度 | 0.52 | 越高，跨链接的颜色混合越明显 |
| 混合距离 | 4 | 颜色沿多少层链接传播 |
| 弱化外围节点 | 0.18 | 让大型枢纽更加突出 |

大量调整链接后，可以运行命令：
**Graph Communities: Recompute graph communities**。
插件也会在元数据索引完成或 Markdown 文件变化后自动重新计算。

## 算法原理

1. 根据 Obsidian 已解析链接构建带权无向图；两篇笔记之间的重复链接会增加边权。
2. 使用确定性的 Louvain 社区发现算法识别连接紧密的群组。
3. 保留主要社区，将较小的连接碎片合并到关系最强的相邻社区。
4. 选择社区中加权连接度最高的笔记作为枢纽。
5. 在指定的链接层数内传播社区亲和度向量。
6. 根据每个节点的社区亲和度在线性 RGB 空间混合颜色，形成与关系结构对应的自然过渡色。

该模型只分析链接结构，不读取笔记语义，也不需要云端嵌入模型。

## 隐私与安全

- 全部计算在 Obsidian 本地完成。
- 不发送网络请求，不收集分析数据，不需要 API Key。
- 不创建、修改、移动或删除笔记。
- 插件设置使用 Obsidian 标准插件数据机制保存。
- 禁用或卸载插件后，会恢复主题原本的节点颜色。

## 兼容性说明

Obsidian 暂未通过公开插件 API 提供逐节点着色能力，因此本插件与其他图谱着色插件一样，
需要访问图谱渲染器的内部节点和连线对象。未来 Obsidian 升级后可能暂时需要兼容更新；
即使出现兼容问题，也只会导致颜色不显示，不会影响知识库内容。

## 开发与测试

```bash
# 从源码生成可安装的 main.js
npm run build

# 运行语法检查和确定性单元测试
npm run verify

# 对真实知识库运行只读冒烟测试
node scripts/vault-smoke.mjs /path/to/vault
```

## 许可证

MIT
